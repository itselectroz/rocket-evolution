const maxVelocity = 4;
const maxForce = 0.2;
const mutationRate = 0.01; // 1%
const lifespan = 500; // how many ticks do the rockets have?

let tickSpeed = 400; // 60 ticks / second     not constant as I might add a speed up option